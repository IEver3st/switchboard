# Switchboard 0.9.9

- Compressed clip and montage exports now recover automatically when the first
  encode exceeds the selected file size.
- Recovery preserves the full timeline, audio, and edits, verifies the completed
  file size, and remains cancellable.
- Exports that already fit keep the normal fast path. Automatic recovery can
  take longer because it uses software encoding.
